/**
 * Formatage des dates côté web — toujours en heure de Paris (`@equime/shared`),
 * quel que soit le fuseau du navigateur.
 */
export {
  formatDate,
  formatDateLong,
  formatDateTime,
  formatDayShort,
  formatHours,
  formatSlot,
  formatTime,
} from '@equime/shared';
